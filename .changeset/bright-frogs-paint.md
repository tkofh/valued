---
'valued': patch
---

add `useValued` Vue composable at `valued/vue`. preserves referential identity across structurally equal parses, supports `defaultValue` and `holdOnError` for failure handling. `vue` is declared as an optional peer dependency
